<!-- <%= form_with(model: folder) do |form| %>
  <% if folder.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(folder.errors.count, "error") %> prohibited this folder from being saved:</h2>

      <ul>
        <% folder.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
-->


<%= form_for @folder do |form| %>
    <%= form.text_field :name %>
   <%= form.fields_for :documents do |doc_subform| %>
   <%=  doc_subform.text_field :title %>
   <% end %>
   <%= form.submit "Create"%>
<% end %>
