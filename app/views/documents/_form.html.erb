<!-- <%= form_with(model: document) do |form| %>
  <% if document.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(document.errors.count, "error") %> prohibited this document from being saved:</h2>

      <ul>
        <% document.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.hidden_field :folder_id , :value=>params[:folder_id] %>
<%= form.text_field :title %>
<!-- <%= form.select :folder_id, options_for_select(Folder.all.map{|s|[s.name, s.id]}) %>
<%= form.collection_select(:folder_id, Folder.all, :id, :name) %>
  <div>
    <%= form.submit "Create new document", {class: "btn btn-accent"}%>
  </div>
<% end %>
-->


<%= form_with(model: document) do |form| %>
<% if document.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(document.errors.count, "error") %> prohibited this document from being saved:</h2>

      <ul>
        <% document.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <% %>
    <%= form.text_field :title %>
    <%= form.text_field :folder_id %>
    <%= form.file_field :file%>
   
   <%= form.submit "Create"%>
<% end %>